# Lucy configuration

`Lucy-Settings.json` is the final VS Code settings snapshot used with the active Lucy layers. It is JSON-with-comments in the same format VS Code uses for its live `settings.json`.

The snapshot includes the repository-relative custom CSS/JS import targets after they are converted to absolute `file:///` URLs for VS Code.
